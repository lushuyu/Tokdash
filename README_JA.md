<p align="center">
  <a href="README.md">English</a> &nbsp;|&nbsp; <a href="README_CN.md">中文</a> &nbsp;|&nbsp; <a href="README_JA.md">日本語</a> &nbsp;|&nbsp; <a href="README_KO.md">한국어</a> &nbsp;|&nbsp; <a href="README_ES.md">Español</a> &nbsp;|&nbsp; <a href="README_PT.md">Português</a>
</p>

<p align="center">
  <a href="https://tokdash.github.io/"><img src="https://raw.githubusercontent.com/JingbiaoMei/tokdash/main/docs/assets/tokdash_logo_full.png" alt="Tokdash" width="420" /></a>
</p>

<p align="center">
  <b>AI コーディングツール向けのローカル トークン・コスト ダッシュボード</b>
</p>

<p align="center">
  <a href="https://opencode.ai/" title="OpenCode"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/opencode.png" alt="OpenCode" height="34"></a>
  <a href="https://openai.com/codex/" title="Codex"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/codex.png" alt="Codex" height="34"></a>
  <a href="https://www.claude.com/product/claude-code" title="Claude Code"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/claude.png" alt="Claude Code" height="34"></a>
  <a href="https://github.com/google-gemini/gemini-cli" title="Gemini CLI"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/gemini.png" alt="Gemini CLI" height="34"></a>
  <a href="https://antigravity.google/" title="Antigravity"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/antigravity.png" alt="Antigravity" height="34"></a>
  <a href="https://openclaw.ai/" title="OpenClaw"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/openclaw.png" alt="OpenClaw" height="34"></a>
  <a href="https://github.com/MoonshotAI/kimi-cli" title="Kimi CLI"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/kimi.png" alt="Kimi CLI" height="34"></a>
  <a href="https://mimo.xiaomi.com/coder" title="MiMo Code"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/mimo.png" alt="MiMo Code" height="34"></a>
  <a href="https://grok.com/build" title="Grok Build"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/grok.png" alt="Grok Build" height="34"></a>
  <a href="https://pi.dev/" title="Pi"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/pi.png" alt="Pi" height="34"></a>
  <a href="https://omp.sh/" title="omp"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/omp.png" alt="omp" height="34"></a>
  <a href="https://kilocode.ai/" title="Kilo Code"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/kilocode.png" alt="Kilo Code" height="34"></a>
  <a href="https://cline.bot/" title="Cline"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/cline.png" alt="Cline" height="34"></a>
  <a href="https://github.com/features/copilot" title="GitHub Copilot CLI"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/copilot.png" alt="GitHub Copilot CLI" height="34"></a>
  <a href="https://hermes-agent.nousresearch.com/" title="Hermes"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/hermes.png" alt="Hermes" height="34"></a>
  <a href="https://github.com/deepseek-ai/deepseek-harness" title="DeepSeek Harness"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/dsh.png" alt="DeepSeek Harness" height="34"></a>
  <a href="https://reasonix.io/" title="Reasonix"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/reasonix.png" alt="Reasonix" height="34"></a>
  <a href="https://zcode.z.ai" title="ZCode"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/zcode.png" alt="ZCode" height="34"></a>
  <a href="https://www.workbuddy.ai/" title="WorkBuddy"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/workbuddy.png" alt="WorkBuddy" height="34"></a>
  <a href="https://qoder.com/" title="Qoder IDE"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/qoder-ide.png" alt="Qoder IDE" height="34"></a>
  <a href="https://qoder.com/" title="Qoder CLI"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/qoder-cli.png" alt="Qoder CLI" height="34"></a>
  <a href="https://zed.dev/" title="Zed"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/zed.png" alt="Zed" height="34"></a>
  <a href="https://github.com/QwenLM/qwen-code" title="Qwen Code"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/qwen-code.png" alt="Qwen Code" height="34"></a>
  <a href="https://charm.land/crush" title="Crush"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/crush.png" alt="Crush" height="34"></a>
  <a href="https://dev.meta.ai/docs/muse-code" title="Muse Code"><img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/agents/pills/muse.png" alt="Muse Code" height="34"></a>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/FastAPI-009688?style=flat&logo=fastapi&logoColor=white" alt="FastAPI" />
  <img src="https://img.shields.io/badge/Python-3.10+-3776AB?style=flat&logo=python&logoColor=white" alt="Python" />
  <img src="https://img.shields.io/badge/License-MIT-green?style=flat" alt="License" />
  <a href="https://tokdash.github.io/"><img src="https://img.shields.io/badge/Website-tokdash.github.io-1E40AF?style=flat&logo=githubpages&logoColor=white" alt="Website" /></a>
  <a href="https://tokdash.github.io/demo/"><img src="https://img.shields.io/badge/Live%20Demo-tokdash.github.io%2Fdemo-F59E0B?style=flat&logo=githubpages&logoColor=white" alt="Live Demo" /></a>
</p>

<p align="center">
  <b>インストールせず試す → <a href="https://tokdash.github.io/demo/">tokdash.github.io/demo</a></b>
</p>

> [!NOTE]
> **複数のマシンをひとつのダッシュボードで。** 設定から WSL・Mac・その他のマシンで動く Tokdash インスタンスを追加できます。概要・セッション・統計は選択したサーバーを合算し、Servers タブはマシンを並べて比較し、Quota タブは各プロバイダーのウィンドウバーとリセットカウントダウンをマシンごとにまとめて表示します。[リモートアクセス →](docs/guides/REMOTE_ACCESS.md) · [クォータ追跡 →](#クォータ追跡任意)
>
> **同じ著者の別プロジェクト: [Cosyncing](https://github.com/cosyncing/cosyncing)。** エージェントの同期と制御 — CLI から GUI へ、デスクトップからスマホへ。中断した箇所からどこでも再開できます。Cosyncing はあなたのネットワーク内でコーディングエージェントを同期します。

> [!TIP]
> **Tokdash Companion が Microsoft Store に登場。** ダッシュボードを開いたままにせずに、Windows の通知領域や macOS のメニューバーから今日の支出とサブスクリプションクォータを確認できます。[Microsoft Store で入手](https://apps.microsoft.com/detail/9ppnmpdq8b52) · [スクリーンショット、ダウンロード、セットアップ →](#tokdash-companion-ステータスバーアプリ)

## 目次

- [機能](#機能)
- [対応クライアント](docs/reference/SUPPORTED_CLIENTS.md)
- [Tokdash Companion ステータスバーアプリ](#tokdash-companion-ステータスバーアプリ)
- [クイックスタート](#クイックスタート)
  - [プラットフォーム対応](#プラットフォーム対応)
- [設定](#設定)
- [プライバシーとセキュリティ](#プライバシーとセキュリティ)
- [API（ローカル）](#apiローカル)
- [コスト精度について](#コスト精度について)
- [履歴の保持](#履歴の保持)
- [ロードマップ](#ロードマップ)
- [コントリビューティング / セキュリティ](#コントリビューティング--セキュリティ)
- [ドキュメント](#ドキュメント)
- [プロジェクト構成](#プロジェクト構成)
- [ライセンス](#ライセンス)

## 機能

- **正確なトークン数**: 入力/出力/キャッシュのトークン内訳
- **ステータスライン統合** *[新]*: Claude Code（ローカル HTTP エンドポイントに到達できるならどのエージェントでも）のステータスラインにリアルタイムのトークン使用量インジケーターを設置 — [ステータスライン統合](#ステータスライン統合)を参照
- **貢献カレンダー**: Tokens/Cost/Messages 指標付きの 2D ヒートマップ + 3D 等角ビュー
- **セッションエクスプローラー**: セッションごとのドリルダウン
- **Report タブ** *[新]*: 自分のエージェント活動を週 / 月 / 年初からの観点でまとめたレポート。詳細度の違う2種類の共有カード付きで、書き出すと常にライトとダークの両方の PNG ができます
- **Quota タブ** *[新]*: Codex、Claude Code、Antigravity のサブスクリプションウィンドウバーとリセットカウントダウン。Codex ウィンドウはローカルログからそのまま動作。Codex のリセットクレジット、メーターリング機能、すべての Claude/Antigravity クォータにはオプトインの[ライブポーリング](#クォータ追跡任意)が必要
- **Companion ステータスバーアプリ** *[新]*: macOS メニューバーまたは Windows 通知領域から支出とサブスクリプションクォータを確認 — Windows 版は[Microsoft Store](https://apps.microsoft.com/detail/9ppnmpdq8b52)に対応 — [スクリーンショットとダウンロード](#tokdash-companion-ステータスバーアプリ)
- **マルチサーバービュー**: 設定から WSL、macOS、その他の Tokdash サーバーを追加。任意の選択で利用量を統合しつつ、クォータはマシン単位でグループ化。[リモートアクセス](docs/guides/REMOTE_ACCESS.md)を参照。
- **テーマとアプリの洗練**: 17 種類のスタイルテーマ、ライト/ダークモード、PWA インストール対応、6 ヶ国語の UI（English / 中文 / 日本語 / 한국어 / Español / Português）

### クライアント対応マトリックス

| クライアント | 使用量とコスト | セッションエクスプローラー |
|---|:---:|:---:|
| OpenCode | ✅ | ✅ |
| Codex | ✅ | ✅ |
| Claude Code | ✅ | ✅ |
| Gemini CLI | ✅ | — |
| Antigravity CLI | ✅ | ✅ |
| OpenClaw | ✅ | ✅ |
| Kimi Code / Kimi CLI | ✅ | ✅ |
| MiMo Code | ✅ | ✅ |
| Grok Build | ✅ | ✅ |
| Pi | ✅ | ✅ |
| **omp** | ✅ | ✅ |
| **Kilo Code** | ✅ | ✅ |
| **Cline** | ✅ | ✅ |
| GitHub Copilot CLI | ✅ | — |
| Hermes | ✅ | ✅ |
| DeepSeek Harness | ✅ | ✅ |
| Reasonix | ✅ | ✅ |
| **ZCode** | ✅ | ✅ |
| **WorkBuddy** | ✅ | ✅ |
| **Qoder IDE** | ✅ | ✅ |
| **Qoder CLI** | ✅ | ✅ |
| **Zed** | ✅ | — |
| **Qwen Code** | ✅ | ✅ |
| **Crush** | ✅ | — |

ローカルデータパス、オーバーライド、ソース固有の集計に関する注記については [対応クライアント](docs/reference/SUPPORTED_CLIENTS.md) を参照。

<p align="center">
  <b>概要</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-overview-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-overview-en-light.png" alt="Tokdash overview dashboard - click for live demo" width="860" />
    </picture>
  </a>
</p>
<p align="center">
  <b>セッション</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-sessions-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-sessions-en-light.png" alt="Tokdash sessions view - click for live demo" width="860" />
    </picture>
  </a>
</p>
<p align="center">
  <b>月間使用量ヒートマップ</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-heatmap-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-heatmap-en-light.png" alt="Tokdash monthly usage heatmap - click for live demo" width="860" />
    </picture>
  </a>
</p>
<p align="center">
  <b>年間使用量ヒートマップ</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-heatmap-year-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-heatmap-year-en-light.png" alt="Tokdash yearly usage heatmap - click for live demo" width="860" />
    </picture>
  </a>
</p>
<p align="center">
  <b>クォータ追跡</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-quota-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-quota-en-light.png" alt="Tokdash quota tracking - click for live demo" width="860" />
    </picture>
  </a>
</p>
<p align="center">
  <b>Codex クォータとリセットクレジット</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-quota-codex-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-quota-codex-en-light.png" alt="Tokdash Codex quota and reset credits - click for live demo" width="440" />
    </picture>
  </a>
</p>
<p align="center">
  <b>複数サーバー、ひとつの合計</b><br />
  <a href="https://tokdash.github.io/demo/">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-servers-en-dark.png" />
      <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-servers-en-light.png" alt="Tokdash Servers tab comparing four machines - click for live demo" width="860" />
    </picture>
  </a>
</p>

## Tokdash Companion ステータスバーアプリ

Tokdash Companion ステータスバーアプリは、macOS のメニューバーアプリ、Windows の通知領域アプリとして動作する、任意のネイティブアプリです。フルダッシュボードを開いたままにせずに、Tokdash サービスのコンパクトな読み取り専用ビューを提供します。

<p align="center">
  <a href="docs/assets/companion/demo-mac.png">
    <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/companion/demo-mac.png" alt="Tokdash Companion Status Bar App on macOS" width="360" />
  </a>
  &nbsp;&nbsp;
  <a href="docs/assets/companion/demo-win.png">
    <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/companion/demo-win.png" alt="Tokdash Companion Status Bar App on Windows" width="360" />
  </a>
</p>
<p align="center">
  <sub><b>macOS</b> メニューバー &nbsp;&nbsp;&nbsp;&nbsp; <b>Windows</b> 通知領域</sub>
</p>

- 本日のコスト、トークン、メッセージ数、月内使用量
- 複数の Tokdash エンドポイントからの統合合計とサーバー別グループのクォータ
- Codex、Claude、Kimi、MiniMax、Antigravity、Grok、Z.ai のクォータウィンドウ
- 相対リセット時間と任意の低クォータ通知
- 任意のログイン時起動
- システム言語の自動検出、および English / 简体中文 / 日本語 / 한국어 / Español / Português
- テレメトリなし、認証情報の探索なし、ポートスキャンなし、直接ログパースなし

### ダウンロード

**Windows — Microsoft Store**（推奨）

<p>
  <a href="https://apps.microsoft.com/detail/9ppnmpdq8b52?mode=direct">
    <picture>
      <source media="(prefers-color-scheme: dark)" srcset="https://get.microsoft.com/images/en-us%20light.svg" />
      <img src="https://get.microsoft.com/images/en-us%20dark.svg" alt="Get Tokdash Companion from the Microsoft Store" width="200" />
    </picture>
  </a>
</p>

Microsoft による署名済みで、Store 経由で更新されます。Windows 11 が必要。

**直接ダウンロード** — **[Tokdash Companion 1.0.0](https://github.com/JingbiaoMei/Tokdash/releases/tag/companion-v1.0.0)**:

| プラットフォーム | ダウンロード | 要件 |
|---|---|---|
| macOS | ユニバーサル DMG（`arm64` + `x86_64`） | macOS 14 以降 |
| Windows | 自己完結型ポータブル ZIP（`x64`） | Windows 11。Windows on Arm は x64 エミュレーションで利用可能 |

> [!WARNING]
> GitHub Releases のバイナリは**未署名**です。macOS の Gatekeeper と Windows の SmartScreen が未知の発行元の警告を表示します。このリポジトリからのみダウンロードし、同梱の `SHA256SUMS` を検証して、リリースを信頼できる場合に限り続行してください。Microsoft Store ビルドは Microsoft による署名済みで影響を受けません。macOS の署名とノタリゼーションは今後のリリースで対応予定です。

### セットアップ

1. [クイックスタート](#クイックスタート)を使って **Tokdash 1.5.2 以降**をインストールし、起動します。
2. Companion をインストールします。Windows では上記の Microsoft Store リンクを使用してください。直接ダウンロードの場合は、自分のプラットフォーム用のアセットを取得し、`SHA256SUMS` と照合して検証します。
3. 直接ダウンロードの場合: macOS では DMG を開いて `TokdashCompanion` をアプリケーションにドラッグ。Windows では ZIP を安定したディレクトリに展開して `TokdashCompanion.exe` を実行。
4. Companion はデフォルトで `http://127.0.0.1:55423` に接続します。設定を開くと、プライベートな Tailscale Serve URL を含む明示的な Tokdash エンドポイントの追加、テスト、名前付け、有効化、削除を行えます。

Companion が通信するのは、あなたが設定した Tokdash エンドポイントのみです。低クォータ通知とログイン時起動はどちらもオプトインで、デフォルトは無効です。チェックサム、更新、削除の手順については [companion リリースガイド](companion/docs/RELEASE.md) を参照してください。

## クイックスタート

### プラットフォーム対応

- **Linux（WSL2 含む）**: 対応
- **macOS**: 対応
- **Windows（ネイティブ）**: 実験的

### 前提条件

- Python **3.10 以降**
- [対応クライアント](docs/reference/SUPPORTED_CLIENTS.md)が 1 つ以上インストールされていること

### インストール

推奨の隔離インストール:

```bash
pipx install tokdash
```

pipx を使わない場合:

```bash
python3 -m pip install --user tokdash
```

### 初回実行

オンボーディングウィザードを実行:

```bash
tokdash setup
```

ウィザードはプラットフォームがサポートする場合に可逆なユーザーレベルのバックグラウンドサービスを設定し、ダッシュボード URL（デフォルト: `http://127.0.0.1:55423`）を表示します。サポートされるサービスマネージャがない場合、セットアップ状態を記録し、フォアグラウンド実行の案内を表示します。localhost 優先のデフォルトを使用し、ローカルサービスには `sudo` を必要としません。`--purge` 付きでアンインストールしない限り、使用履歴は保持されます。

すべてのネットワークインターフェースへ書き込み無効でダッシュボードを明示的に公開するには `tokdash setup --bind 0.0.0.0` を実行してください。事前に[リモートアクセスガイド](docs/guides/REMOTE_ACCESS.md)を確認してください。

エージェント、スクリプト、バンドルからの非対話セットアップ:

```bash
tokdash setup --auto --json
```

セットアップが何を変更するかプレビューする:

```bash
tokdash setup --dry-run
```

### 検証

```bash
tokdash doctor
```

`doctor` はランタイム、バックグラウンドサービス、設定ポート、データパス、更新チェックの状態を確認します。自動化には `tokdash doctor --json` を使います。

### 更新または削除

```bash
tokdash update       # 管理対象ランタイムをアップグレードし、可能ならサービスを再起動
tokdash uninstall    # setup が作成したものだけを正確に破棄。デフォルトでは使用履歴を保持
```

`update` が操作するのは、Tokdash が安全に管理できるインストール方法のみです。あなたのランタイムが Tokdash が所有していないパッケージマネージャでインストールされた場合、その環境を変更する代わりに、手動手順の正確な案内を表示します。管理対象ランタイムでは、`update` はアップグレード前後の Tokdash バージョンを報告します。バージョンが変わらない場合、新規パッケージのインストールを示唆するのではなく、すでにそのバージョンである旨を伝えます。

<details>
<summary>既存インストール: v1.0 以前からの移行</summary>

オンボーディングフロー以前に Tokdash をインストールした場合は、まずアップグレードしてください:

```bash
pipx upgrade tokdash
# または: python3 -m pip install --user -U tokdash
```

その後、バックグラウンドサービスの管理を Tokdash に委ねたいときに `tokdash doctor` と `tokdash setup` を実行してください。手書きの systemd や launchd サービスがすでにある場合、setup はそれを**黙って置き換えません**: マークされていない `tokdash.service` / plist ファイルはデフォルトで拒否します。そのサービスの管理を自分で続けるか、setup 前に削除するか、`tokdash setup --dry-run` を確認してから `tokdash setup --force` を実行してください。`--force` は、ポート `55423` を占有しているが新しい `/health` フィンガープリントを公開していない 1.0 以前のサービスも扱います: 既存の `tokdash.service` を書き換えて再起動します。サービス作成をスキップするには `tokdash setup --no-service` を使います。

現在のセットアップが conda / システム / ユーザー pip のインタプリタを使っていて、今後のアップグレードを `tokdash update` に管理させたい場合は、サービスを Tokdash の setup 管理 venv へ移行してください:

```bash
# 実行しようとしている tokdash コマンドをアップグレード。例:
python3 -m pip install --user -U tokdash
# または、conda base インストールの場合:
conda run -n base python -m pip install -U tokdash
tokdash setup --runtime venv --force
tokdash doctor
```

これにより、使用履歴は `~/.tokdash` 配下に保持され、ユーザーサービスは `~/.tokdash/runtime/python-venv/bin/python -m tokdash` を実行するように書き換えられ、今後の `tokdash update` がその管理 venv をアップグレードしてサービスを再起動できます。pipx でインストールした場合は、pipx ランタイムを維持したまま `tokdash update` や `pipx upgrade tokdash` でアップグレードし続けることもできます。

</details>

### リモートアクセス

Tokdash はデフォルトでループバックにバインドされたままです。対話的な `tokdash setup` は明示的な確認の後、Tailscale Serve を設定でき、Windows または他の tailnet デバイスからプライベートな HTTPS 読み取りアクセスを提供します。認証付きの書き込みアクセスが必要な場合は SSH フォーワードを使用してください。明示的な `--bind 0.0.0.0` は読み取り専用のネットワークアクセスを提供しますが、非認証のダッシュボードを到達可能なすべてのインターフェースに公開します。

セットアップコマンド、WSL2 の案内、アクセス URL、書き込みの動作、セキュリティ上のトレードオフについては **[`docs/guides/REMOTE_ACCESS.md`](docs/guides/REMOTE_ACCESS.md)** を参照。

### フォアグラウンドフォールバック

一度きりのフォアグラウンドプロセスで十分な場合:

```bash
tokdash serve
```

`http://127.0.0.1:55423` を開きます。デフォルトポートが使用中の場合は `tokdash serve --port <port>` を使います。

ランタイムの選択肢、WSL/systemd の動作、macOS の launchd、Tailscale、バンドリング、アップデートチェック、安全なアンインストールの挙動を含む、オンボーディングの全詳細については **[`docs/guides/ONBOARDING.md`](docs/guides/ONBOARDING.md)** を参照してください。

### OpenClaw ダイジェスト（定期レポート）

Tokdash はローカル API を定期照会することで、OpenClaw の日次 / 週次 / 月次使用量レポートを支えることができます。

#### 人間向け

次のプロンプトを LLM エージェント（Claude Code、AmpCode、Cursor など）にコピー＆ペーストしてください:

```text
Install and configure scheduled Tokdash usage reports for OpenClaw by following the instructions here:
https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/guides/agents/openclaw_reporting/AGENTS.md

Or read the guide yourself, but seriously, let an agent do it.
```

#### LLM エージェント向け

インストールガイドを取得して、それに従ってください:

```bash
curl -s https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/guides/agents/openclaw_reporting/AGENTS.md
```

### ステータスライン統合

ローカル API が、コーディングエージェント（Claude Code など）のステータスライン項目としてリアルタイムのトークン / コスト統計を表示します。

**完成済みテンプレート**は [`docs/guides/statusline/`](docs/guides/statusline/) にあります — 1 つを `~/.claude/scripts/` にコピーし、`~/.claude/settings.json` に `statusLine` ブロックを追加してください:

- [`statusline-minimal.sh`](docs/guides/statusline/statusline-minimal.sh) → 1 行: `[Claude Sonnet 4.6] 📁 myproject | 📊 12.3M ($4.56) today`
- [`statusline-full.sh`](docs/guides/statusline/statusline-full.sh) → 本日 + 週合計とツール別 top 3 内訳付きの 4 行ダッシュボード
- [`statusline.ps1`](docs/guides/statusline/statusline.ps1) → minimal テンプレートと同じ 1 行出力。Windows ネイティブの Claude Code 用（PowerShell、`curl`/`jq` 不要）

すべて読み取り専用・localhost のみで、Tokdash が起動していない場合は黙って失敗します。インストール / 設定は[フォルダ README](docs/guides/statusline/README.md)、エンドポイント参照は [`docs/reference/API.md`](docs/reference/API.md) を参照。

自作したい場合? エージェントにこのプロンプトを渡し、[`docs/reference/API.md`](docs/reference/API.md) に向かわせましょう:

> *"I would like to add a statusline item from the tokdash endpoint's API; it should show the total tokens used today."*

<p align="center">
  <img src="https://raw.githubusercontent.com/JingbiaoMei/Tokdash/main/docs/assets/demo-statusline.png" alt="Tokdash statusline integration example" width="900" />
</p>

## 設定

Tokdash は**デフォルトで localhost のみ**です。

- `TOKDASH_HOST`（デフォルト: `127.0.0.1`）
- `TOKDASH_PORT`（デフォルト: `55423`）
- `TOKDASH_CACHE_TTL`（デフォルト: `600` 秒）
- `TOKDASH_CACHE_MAX_ENTRIES`（デフォルト: `256`） — キャッシュされた API レスポンスとそのキーごとのアイドルロックに上限を設ける
- `TOKDASH_COMPUTE_CONCURRENCY`（デフォルト: `2`） — 同時実行される重い履歴再パースの上限。超過したコールドリクエストはサーバーを飽和させる代わりに高速な `503` を返す
- `TOKDASH_STARTUP_WARM_JOIN_SECONDS`（デフォルト: `30` 秒） — 起動時にウォームアップ中のキーの最初のリクエストが、`503` を返す代わりにその充填を待つ時間。キーごとに待てるのは最大 1 リクエスト
- `TOKDASH_LIMIT_CONCURRENCY`（デフォルト: `64`） — uvicorn 接続の上限（バックプレッシャー）
- `TOKDASH_KEEPALIVE`（デフォルト: `5` 秒） — uvicorn のキープアライブタイムアウト
- `TOKDASH_ALLOW_ORIGINS`（カンマ区切り、デフォルト: 空）
- `TOKDASH_ALLOW_ORIGIN_REGEX`（デフォルトの CORS ポリシーは localhost/127.0.0.1 と同じ tailnet からの Tailscale Serve 読み取りを許可。どちらかの CORS オプションを設定するとそのデフォルトポリシーを置き換える）
- `TOKDASH_NO_RETENTION_NOTICE`（`1` にすると `tokdash serve` で表示される履歴保持のリマインダーを無音にする）
- `TOKDASH_SETUP_NO_OPEN`（`1` にすると `tokdash setup` 末尾の任意のブラウザ起動をスキップする）

セッションのアクティブ時間（推定値）:

すべてのセッションが `span_ms` と並んで `active_ms` を報告します。Span は初回から最終イベントまで。アクティブ時間はアイドルを差し引いて計算され、連続するトークンイベント間の各ギャップはアイドル上限までしかカウントしないため、終日開き放しにしたセッションが 14 時間分のセッションと読まれなくなります。

これは推定値であり、API もそれを示しています: `summary.active_time_estimated` は `true`、`summary.active_time_method` は `capped-inter-event-gap` です。限界は手法に由来します — イベント間の短い停止は作業と区別できません、上限を超える単一の操作は上限で切り詰められ、1 つだけのトークンイベントを持つセッションはその前に何もないので 0 と計測されます。

並行作業は 2 通りでカウントされます。`active_ms` は重複を 1 回のみカウントしたクロック時間。`active_ms_sum` は重複を足し上げたもの、つまりエージェント時間です。両方が `summary` にセッションごと・ツールごとに登場します。Kimi エージェントと Claude サブエージェントはメインエージェントと並走し、それぞれ独立したストリームとして計時されます: 1 分間並行して働いたサブエージェントはエージェント時間に 1 分を加え、クロック時間には何も加えません。

- `TOKDASH_ACTIVE_GAP_CAP_SECONDS`（デフォルト: `300`） — 秒単位のアイドル上限。これを超えるギャップは上限分のみ寄与。1 秒〜6 時間にクランプされる。

永続使用量 DB（デフォルトで有効）:

Tokdash はデフォルトで `~/.tokdash/usage.sqlite3` にローカルの SQLite インデックスを維持します。パース済みトークン行と Codex/Claude/Kimi/DeepSeek Harness/Reasonix のセッション要約を保存し、繰り返しされるダッシュボードや API の読み取りが、すべてのソースログを再パースする代わりにインデックス付き SQL を使えるようにします。ソースログが引き続き正のソースであり、DB はローカルのパフォーマンスインデックスです。無効化または利用不可能な場合はライブパースにフォールバックします。

キャッシュされたセッション行は価格に対して中立です: 各ターンの課金入力（モデル、フレッシュ入力、キャッシュ読み書き、出力）を持ち、コストは読み取り時にそのプロセスが読み込んだ価格で計算されます。そのためレートを書き換えるとログの数 GB を再読み取りする代わりに即時に再価格付けされ、1 つのデータベースを共有する 2 つの Tokdash バージョンは価格を理由に互いの行を無効化しません。ただし共有が安全なのは、両方のビルドが同じデータベーススキーマをサポートしている間だけです: スキーママイグレーションは前方へのみ実行されるため、新しいビルドがファイルをマイグレーションすると、古いビルドはアップグレードするまでファイルをオープンすることを拒否し、バージョンが一致するまですべてのキャッシュ読み取りが失敗します。`tokdash doctor` はディスク上のスキーマと実行中ビルドがサポートするスキーマを並べて報告します。パーサーとソースファイルの変更は通常どおり再パースされます。これ以前に書かれた行（ログが消えても `TOKDASH_USAGE_DB_DURABLE` で保持されたものを含む）は保存された合計から再価格付けされ、同じ数値は再現されますが、Claude や Kimi のキャッシュ書き込みとフレッシュ入力を分離できません。その区別を回復できるのはそのログの再パースだけです。Codex は `provider/model` で課金され素のモデル名を保存するため、古い行は再利用されるのではなく 1 回だけ再パースされます。

インストール済サービスのデータベースを絶対に移行させたくない場合は、チェックアウトを自分のデータディレクトリに対して実行してください:

```bash
TOKDASH_DATA_DIR=output/dev-data PYTHONPATH=src python3 main.py
```

- `TOKDASH_USAGE_DB`（デフォルト: `1`） — `0`、`false`、`no`、`off` のいずれかに設定すると永続使用量 DB を無効化
- `TOKDASH_DATA_DIR`（デフォルト: `~/.tokdash`） — Tokdash ローカル状態のベースディレクトリ
- `TOKDASH_USAGE_DB_PATH`（デフォルト: `$TOKDASH_DATA_DIR/usage.sqlite3`） — 明示的な SQLite ファイルパス
- `TOKDASH_USAGE_DB_DURABLE`（デフォルト: `1`） — ソースファイルが一時的に消えたりパーサーが行を返さなくなっても、インデックス済みの行を保持。厳密なソース置き換えにするには `0`
- `TOKDASH_USAGE_DB_WATCH`（デフォルト: `0`） — `1` にすると `tokdash serve` 内でバックグラウンド同期ループを実行
- `TOKDASH_USAGE_DB_WATCH_INTERVAL`（デフォルト: `30` 秒） — `tokdash db watch` と serve 時のウォッチループの同期間隔

DB 維持コマンド:

```bash
tokdash db status --pretty
tokdash db sync --pretty
tokdash db verify --verify-period today --pretty
tokdash db repair --dry-run --pretty
tokdash db resync --pretty
tokdash db watch --pretty
```

Tailscale Serve、SSH フォーワード、明示的なネットワークバインドによるリモートアクセスについては、[`docs/guides/REMOTE_ACCESS.md`](docs/guides/REMOTE_ACCESS.md) を参照。対話的な `tokdash setup` はオプトインの後、Tailscale Serve ルールを設定して記録できます。

デフォルトで `tokdash serve` は起動時に一度だけブラウザでダッシュボードを開きます。無効にするには `--no-open` を付けてください（ヘッドレス / SSH 環境やバックグラウンドサービスのテンプレートでは自動的にスキップされます）。

## プライバシーとセキュリティ

- **テレメトリなし**: Tokdash はあなたのデータを意図的にどこにも送信しません。
- **ローカルパース**: 使用量はローカルのセッションファイルから計算されます（[対応クライアント](docs/reference/SUPPORTED_CLIENTS.md)を参照）。
- **任意のクォータポーリング**: Quota タブはデフォルトでローカルのみ。プロバイダーごとの API ポーリングはタブから、または `tokdash quota consent` で有効化できます。ローカルの CLI 認証情報を使用してそのプロバイダー自身のクォータエンドポイントを呼び出すだけであり、レスポンスはローカルの使用量 SQLite DB に保存されます。
- **サーバー公開**: Tokdash はデフォルトで `127.0.0.1` にバインドします。Tailscale Serve はプライベートな読み取り専用アクセスを、SSH フォーワードは認証付きの書き込みアクセスを、`--bind 0.0.0.0` はすべてのインターフェースで非認証の読み取りを明示的に公開します。[リモートアクセスガイド](docs/guides/REMOTE_ACCESS.md)を参照。

### クォータ追跡（任意）

Quota タブは 2 つのデータソースから、サブスクリプションの利用率ウィンドウとリセットタイマーを表示します。**ローカルログ**（ネットワーク不要）: Codex はセッションファイルに自分のクォータを記録するため、Codex の 5 時間 / 週間ウィンドウはそのまま動作します — ただし Codex を使ったときのみ更新され、ログにはリセットクレジットやメーターリング機能ウィンドウが含まれません。セッションログの Codex 消費量は**大幅に誤りうる推定値**として扱うべきです: 各セッションが最後の取得時のクォータスナップショットをキャッシュし、その後の全メッセージで同じものを再生するため、数値が古い場合があり、リセット境界のノイズがウィンドウをさらに歪めることもあります — Quota タブはこれらのチャートを推定値とラベル付けします。**ライブポーリング**（デフォルト無効、プロバイダーごとにオプトイン）: Tokdash は CLI がすでに持っているサインインで、プロバイダー自身のクォータエンドポイントを呼び出します。より新鮮で、Codex のリセットクレジットとメーターリング機能を追加し、**正確な** Codex 消費量には必須であり、Claude Code、Antigravity、MiniMax、Kimi Code、SuperGrok/Grok Build の唯一のクォータソースです:

Z.ai Coding Plan と OpenCode Go のクォータもライブポーリングからのみ取得できます。

```bash
tokdash quota consent --codex-api on --claude-api on --antigravity-api on
tokdash quota consent --minimax-api on --kimi-api on --grok-api on --zai-api on
tokdash quota consent --opencode-go-api on
tokdash quota consent --credential-scan on   # 開示済みのローカル認証情報リーダーを許可
tokdash quota consent --poll-interval 30      # バックグラウンドポーリング間隔: 15, 30, 60 または 120 分
tokdash quota consent --enabled off           # マスタースイッチ: 全クォータ追跡をオフ
tokdash quota poll
tokdash quota show
```

**マスタースイッチ。** `quota.enabled`（デフォルトでオン）はクォータ関連の全作業 — セッションスキャン、ネットワークポーリング、スナップショット書き込み — を一括でオン / オフにします。Quota タブから、または `tokdash quota consent --enabled on|off` で切り替えます。オフのとき（または `TOKDASH_QUOTA_POLL=0` のキルスイッチが設定されているとき）、バックグラウンドポーラーは完全にアイドルとなり、`GET /api/quota/refresh` は「quota tracking disabled」エラーを返し、タブはデータではなく*クォータ追跡を有効化*カードを表示します。プロバイダーごとのコンセンツキーはより狭いネットワーク限定の意味を維持します。

**ポーリング間隔。** バックグラウンドポーラーはデフォルトで **30 分ごと**にスナップショットします。15/30/60/120 分を Quota タブ、`tokdash setup` 中、または `tokdash quota consent --poll-interval N` で選択でき、`config.json` に `quota.poll_interval_minutes` として保存されます。環境変数 `TOKDASH_QUOTA_POLL_INTERVAL`（秒、下限 300）は保存値を上書きし、タブはどのソースが有効かを表示します。間隔の変更はサーバー再起動なしに次のポーリングサイクルで適用されます。Codex セッションの取り込みは増分です — 履歴の 1 回限りバックフィルの後、各サイクルは増加したセッションファイルのみをテール読み取りするため、定常時のポーリングコストは数ミリ秒です。

固定リセットのクォータウィンドウでは、ポーラーはリセット境界付近でもサンプリングし、履歴がリセット直前的高値とリセット直後のベースラインを捕捉できるようにします。境界サンプリングはデフォルトで有効で、ウィンドウがトリガーしたプロバイダーのみを呼び出し、 近いプロバイダー境界をまとめ、デーモンポーリングサイクル間に最低 300 秒を保持します。`TOKDASH_QUOTA_BOUNDARY_POLL=0` で無効化、`TOKDASH_QUOTA_BOUNDARY_POST=0` でリセット後サンプルのみの無効化、デフォルトの 120 秒リードは `TOKDASH_QUOTA_BOUNDARY_PRE_SECONDS` と `TOKDASH_QUOTA_BOUNDARY_POST_SECONDS` で調整できます。

**複数の Claude Code インストール。** Claude Code は設定ディレクトリごとに 1 つのサブスクリプションを保持するため、`CLAUDE_CONFIG_DIR=~/.claude-academic claude` として実行する 2 つ目のサインインは、独自のウィンドウを持つ別のサブスクリプションです。認証情報スキャンに同意すると、Tokdash は `$CLAUDE_CONFIG_DIR` に加えて独自の `.credentials.json` を持つすべての `~/.claude*` ディレクトリを読み取り、それぞれを個別にポーリングして、そのディレクトリが設定されたプロファイル名（`academic`）で Claude Code カード内にグループ化します。履歴でも区別され、`Claude-academic 5-hour` は `Claude 5-hour` と並ぶ独立した系列になります。サインインが期限切れのインストールは、正常なインストールの数値を隠すのではなく自身の通知を表示し、同じサインインを保持する 2 つのディレクトリは 1 回だけカウントされます。ホームディレクトリの外にあるインストールについては、`TOKDASH_CLAUDE_PROFILES` にパス区切り文字で区切ったディレクトリのリストを設定してください。使用量の集計には変更が不要でした: すべての `~/.claude*` インストール配下のセッションログは以前から集計されています。

ライブポーリングは 2 つの別々の決定を要求します: `quota.credential_scan` は開示済みのローカル認証情報ストアへの読み取り専用アクセスを許可し、その後各 `<provider>_api` キーがそのプロバイダーのネットワークリクエストを許可します。Tokdash はネイティブ CLI の認証 / 設定ファイル、OpenCode の `auth.json` とグローバルプロバイダー設定、アクティブな Claude 設定、CC Switch の `providers` テーブルを読み取り専用の SQLite 接続で読み取ります。プロバイダーのログ、シェルプロファイル、任意の `{file:...}` 参照は決してスキャンしません。MiniMax は `mmx` サインインまたは Token Plan Subscription Key（`MINIMAX_TOKEN_PLAN_GLOBAL_KEY` / `MINIMAX_TOKEN_PLAN_CN_KEY`）を受け付けます。通常の従量課金キーに Token Plan クォータがあるとは限りません。Kimi は Kimi Code サインイン / キー（`KIMI_API_KEY`）を受け付け、Moonshot Open Platform の従量課金キーは受け付けません。SuperGrok/Grok Build のクォータには `$GROK_HOME/auth.json` の xAI OAuth サインインが必要です。通常の xAI API キーではコンシューマー課金にアクセスできません。macOS では Claude Code が 1 回だけの読み取り専用 Keychain 承認を要求することがあります。Tokdash はプロバイダーの認証情報を更新も書き込みもしません。`TOKDASH_QUOTA_POLL=0` は全クォータ追跡のハードキルスイッチです。`tokdash export` はデフォルトでクォータデータを除外します。JSON に含めたいときには意図的に `--include-quota` を使用してください。

Z.ai は `$ZCODE_HOME/v2/config.json`、対応ツールの設定、`ZAI_API_KEY`、または `Z_AI_API_KEY` の Coding Plan キーを受け付け、5 時間 / 週間クレジットウィンドウと従来の MCP 上限を照会します。

OpenCode Go は `OPENCODE_API_KEY` を優先し、未設定の場合は OpenCode の `auth.json` 内の `opencode-go` キーを使って、`opencode.ai/zen/go/v1/usage` からローリング・週間・月間のサブスクリプション使用枠を取得します。Zen の従量課金残高にはキー認証に対応したエンドポイントがないため、追跡しません。

Grok Build のトークン使用量も `$GROK_HOME/logs/unified.jsonl` からローカルにパースされます。その推論レコードはプロンプト、キャッシュ済みプロンプト、補完、推論トークンを公開し、Tokdash は同じ CLI プロセスのモデルイベントを使って帰属させ、通常の価格データベースからコストを計算します。モデルイベントのないレコードは推測価格を割り当てるのではなくスキップされます。

DeepSeek Harness（`dsh`）の使用量とセッションは `$DSH_HOME/sessions/*/*/session.jsonl.zstd`（または非圧縮の `session.jsonl`）からローカルに読み取られ、`DSH_HOME` のデフォルトは `~/.dsh` です。各ログは連結された zstd フレームの列です。Tokdash はすべてのフレームをデコードし、各ステップの初期使用量チャンクを確定済みメッセージに折りたたんで二重計上を避け、フォークセッションの継承プレフィックスをスキップして親子が同じトークンを 2 回課金しないようにします。

Reasonix の使用量とセッションは `$REASONIX_HOME`（デフォルト `~/.reasonix`）からローカルに読み取ります: リクエストごとのトークンは日次 `stats/YYYY-MM-DD.jsonl` ログから、セッション構造は `projects/*/sessions/*.jsonl` から。Reasonix は `config.toml` に名前の出たプロバイダーとやり取りするため、各行の `provider/model` ペアは通常の価格データベースを通じて帰属・価格付けされます。公開レートのないセルフホストモデルはトークンをゼロコストで集計します。Reasonix はリクエストごとに使用量を記録しセッション ID を付与しないため、Session Explorer の行はトークン数なしでターン、プロジェクト、タイミングを表示します — 完全な合計は Overview と Stats にあります。
ZCode の使用量は `$ZCODE_HOME/cli/db/db.sqlite`（デフォルト `~/.zcode/cli/db/db.sqlite`。`ZCODE_HOME` は ZCode 自身の設定に従う）からローカルに読み取ります。各 `model_usage` 行はリトライ込みの 1 つのモデルリクエストで、行の `model_id` は通常の価格データベースで価格付けされ、`provider_id`（例: `builtin:zai-start-plan`）はラベルとして保持されます。ZCode の `input_tokens` はキャッシュ済みと未キャッシュのプロンプトトークンを合計して数えるため、キャッシュ部分は独立したバケットに分離してキャッシュレートを適用し、推論トークンは出力と離して表示しつつ出力レートで課金されます。ZCode は Sessions タブにも登場します: ターンは同じデータベースから読み取られ、同じルールで (ターン, モデル) 単位で課金され、トップレベルセッションのみ。課金トークンを生み出さなかったターンも、計測された時間をそのツールのアクティブ時間に帰属します。ZCode の Coding Plan クォータはリモートアカウントデータであり、ローカルパーサーの範囲外です。

Coding Plan クォータは、個別に同意した Z.ai ライブポーラーから利用できます。

WorkBuddy の使用量は `~/.workbuddy-ai/projects/*/*.jsonl` トランスクリプトからローカルに読み取ります（`WORKBUDDY_DATA_DIR` はカンマ区切りのルートリストを受け取り、他のストア — 例: WSL からの Windows データディレクトリ — を指定できます）。各アシスタントメッセージ行は 1 つのモデル呼び出しで、`prompt_tokens` 内のキャッシュ部分は独立したバケットに分離してキャッシュレートを適用し、推論トークンは出力と離して表示しつつ出力レートで課金されます。モデル ID はそのまま保持されます: 明示的な ID（例: gpt-5.5）は通常の価格データベースで価格付けされ、Auto ルーターのエイリアス（`default-model`）は価格 DB に存在しないためコスト 0.00 です。ターンごとの `credit` 値はメタデータとしてのみ保存され、コストに影響しません。Sessions タブは同じルートから同じトランスクリプト行（計費対象のアシスタント 1 行 = 1 ターン）を読み取ります。

Qoder の使用量は 2 か所からローカルに読み取ります: IDE の SQLite データベース（IDE データディレクトリ配下の `SharedClientCache/cache/db/local.db`。Windows と WSL では QoderCN ビルドが国際版より優先）と CLI の JSONL ログ（`~/.qoder` と `~/.qoder-cn`、加えて `QODER_CONFIG_DIR` とカンマ区切りの `QODER_CLI_HOME`）。IDE 側では全ロールの `chat_message` 行がすべてカウントされ、キャッシュ部分はプロンプトトークンから独立したバケットに分離され、モデルは `model_key`（ルーター名がない場合は `auto`）から。CLI 側では各リクエストのトランスクリプト課金記録がすべてのルートでセグメントトークン記録とマージされます: プロバイダークレジットを持つ行はプロバイダー報告コストを権威として保持し（推定で 1 クレジット $0.01 で換算、`QODER_USD_PER_CREDIT` が推定を上書きし、再価格付けは決してされない）、トークンのみの行は通常の価格データベースで価格付けされます。入力トークンのない記録は既知のコンテキストウィンドウに対して `context_usage_ratio` から復元します — デフォルトでは `auto` が 180,000、`QODER_CLI_CONTEXT_WINDOW` が明示設定された後はすべてのモデル。Qoder IDE は Sessions タブに表示されます: 同じ `chat_message` 行（全ロール、解析可能な 1 行 = 1 ターン）を同じ DB の一時ディレクトリスナップショットから読み取ります。 Qoder CLI にも独自の Sessions パネルがあります: 両ストリームのファイル別候補は Overview と同じ順序・同じ勝者で同じ request_id マージを通過し、クレジット行はプロバイダー報告のコストをそのまま保ち、セグメントのプロジェクトはサニタイズの可逆性がないためサニタイズ済みディレクトリラベルをそのまま表示します。

Zed の使用量は、OS ごとの Zed データディレクトリ配下の `threads/threads.db` からローカルに読み取ります（Linux: `$XDG_DATA_HOME/zed` または `~/.local/share/zed`、`FLATPAK_XDG_DATA_HOME` も考慮; macOS: `~/Library/Application Support/Zed`; Windows: `%LOCALAPPDATA%\Zed`）。エージェントスレッド 1 本が 1 行で、zstd 圧縮された blob（旧形式の行は素の JSON）が `cumulative_token_usage` を保持します。これはスレッド自身の補完ストリームがフィールドごとの高水位で積み上げた値で、キャッシュ排他（input + cacheRead が全プロンプト）のためバケットはそのまま対応します。サブエージェントのスレッドは独立した行で親に畳み込まれないため、トークンを持つ各スレッドはちょうど 1 回だけカウントされます。スレッドは現在のモデル（モデルを切り替えた場合は最後のモデル）で価格付けされ、価格 DB にないセルフホストの ID はコスト 0.00 です。Zed にはデータディレクトリを差し替える環境変数がないため、`--user-data-dir` フラグが文書化された盲点です。Zed は Sessions タブには登場しません。

Qwen Code の使用量は `<base>/projects/*/chats/*.jsonl`（および改名前の `<base>/tmp/*/chats/*.jsonl`）からローカルに読み取ります。base は `$QWEN_RUNTIME_DIR`、次に `$QWEN_HOME`、次に `~/.qwen` の順で解決します — 設定ファイル側の `runtime_base_dir` は Tokdash からは到達できず、文書化された盲点です。ファイルは追記専用でセッションごとに 1 つ、各アシスタントレコードはプロバイダーの `usageMetadata` をそのまま保持します: `promptTokenCount` はキャッシュを含むため、キャッシュ分は独立したバケットに分離してキャッシュレートで課金し、`thoughtsTokenCount` は推論として表示します。各レコードの `uuid` はソース全体で安定したキーです: `/branch` は親のレコード（同じ uuid）をフォーク先のファイルにコピーするため、使用量ストアは最も早い出現でキーの所有者を決め、正規のファイルが削除されたときは残ったコピーを昇格させます。サブエージェントのレコードはセッションファイルを共有し、自動的にカウントされます。コストは価格 DB からのみで、モデルのないレコードは `unknown` として 0.00 で計上します。Sessions タブは同じアシスタントレコードを読み取り、1 件を 1 ターンとして計上します。フォークで複製された履歴は最も早いコピーに帰属します。

Crush の使用量は `$CRUSH_DATA_DIR`（それぞれ `crush.db` を含むデータディレクトリのカンマ区切りリスト）からローカルに読み取ります — 必須です。Crush の既定のデータディレクトリは作業ディレクトリの隣にあるプロジェクトごとの `.crush` で、走査できるグローバルなルートが存在しないためです。データベースは WAL モードのため、ZCode と同じコピー＆スナップショット経路で読み取ります。トークンが 0 でないセッションごとに、その `prompt_tokens`／`completion_tokens` から 1 エントリで、サブエージェントのセッションも含みます: Crush が親セッションに畳み込むのはコストだけでトークンは畳み込まないため、Crush 自身の統計クエリが使う最上位のみの条件（`parent_session_id IS NULL`）ではその使用量が落ちてしまいます。各エントリはそのセッションの最後の非サマリーのアシスタントメッセージに帰属します — 複数モデルのセッションは最後のモデルで価格付けされます。注意点が 3 つあります: カウンターは累積ではなくステップごとに代入されるため、最後のリクエストのコンテキストサイズと最後のターンの出力しか保持せず、複数ステップのセッションでは実際より少なく出ます（Crush v0.91.2 で確認）。プロバイダーが使用量ゼロを報告した場合、トークンが文字数からの推定値になることがあり、DB 上にその印はありません。そしてキャッシュ／推論の内訳は保存されません。タイムスタンプは秒で、行は `updated_at`（最終更新）でバケットされるため、セッションの全期間の合計が 1 日に載ります。`sessions.cost` は無視し、コストは価格 DB からのみ求めます。Crush は Sessions タブには登場しません。

`tokdash setup` は任意のクォータステップ（プロバイダーごとのネットワークコンセンツ、デフォルト No、およびポーリング間隔）を提供し、`tokdash doctor` はクォータの状態を報告します: マスタースイッチ、プロバイダーごとのコンセンツ、キルスイッチ、有効間隔とそのソース、最終ポーリング時刻、保存されたスナップショット数。

クォータスナップショットとその履歴はローカル使用量データベース（`usage.sqlite3`、デフォルトで有効）に保存され、**デフォルトで永久に保持されます** — 古いスナップショットを削除するには `TOKDASH_QUOTA_RETENTION_DAYS` を日数の正数に設定してください。`TOKDASH_USAGE_DB=0` でローカル永続化をオプトアウトすると、Quota タブは主データ経路を失います: スナップショット履歴が保持されず、バックグラウンドポーラーは実行されず、タブは手動 **Refresh**（コンセンツ済みのネットワークプロバイダー）の結果を現在のサーバープロセスの生存期間中インメモリで表示するだけです。通常のクォータ追跡には使用量 DB を有効（デフォルト）にしてください。

## API（ローカル）

Tokdash はローカル HTTP サーバーです。よく使われるエンドポイント:

- `GET /api/usage?period=today|week|month|N`
- `GET /api/usage?date_from=YYYY-MM-DD&date_to=YYYY-MM-DD`
- `GET /api/tools?period=...`（コーディングツールのみ）
- `GET /api/openclaw?period=...`（OpenClaw のみ）
- `GET /api/sessions?tool=codex|claude|opencode|pi_agent|omp|mimo|kimi|dsh|reasonix|zcode|kilocode|grok|hermes|antigravity_cli|cline&period=...`（Codex のレビュー / 許可セッションを含むには `&include_review_sessions=true` を追加。デフォルトでは非表示）
- `GET /api/active-time?period=...`（全セッションツールのアクティブ時間 + ツール別内訳）
- `GET /api/quota` と `GET /api/quota/history`（サブスクリプションクォータスナップショット。ネットワークリフレッシュは書き込みゲート付きでオプトイン）
- `GET /api/stats`（貢献カレンダーと統計）

例:
```bash
curl 'http://127.0.0.1:55423/api/usage?period=today'
```

完全な API 参照: [`docs/reference/API.md`](docs/reference/API.md) — すべてのエンドポイントのスキーマ、パラメータ、レスポンスの形状。

## コスト精度について

トークン数は各クライアントがローカルにログ記録する内容に依存します。コストはデフォルトで同梱の価格データベース（`src/tokdash/pricing_db.json`）、存在すればダッシュボードの価格オーバーライド（`<data_dir>/pricing_db.json`。Pricing タブがそこに書き、同梱レートを完全に置き換える）から計算されます。どちらの場合も実際のプロバイダー価格より遅れる可能性があります — 推定値として使い、重要な場合は請求情報と照合してください。

## 履歴の保持

> [!IMPORTANT]
> **履歴を保持してください。** Claude Code と Gemini CLI はデフォルトで約 30 日以前のローカルセッションを削除するため、Tokdash の過去の数ヶ月が黙って縮小します。

Tokdash は各クライアントの**ローカル**セッションログを読み、ローカルの SQLite パフォーマンスインデックスも保持します。インデックスは Tokdash がすでに確認した行を保持できますが、インデックス化される前に削除されたログを回復することはできず、クライアントの元の履歴を保持する置き代わりではありません。クライアントが Tokdash が同期する前に古いログを削除すると、過去の月が**最初に記録したときより低く**読まれることがあります。デフォルトでこれをやるのはサポートクライアントのうち 2 つだけで、どちらも 1 行で直せます:

- **Claude Code** は起動時に `cleanupPeriodDays`（**デフォルト 30 日**）より古いセッションを削除します。既存の `~/.claude/settings.json`（および任意の代替 `CLAUDE_CONFIG_DIR`）に次を追加:
  ```json
  { "cleanupPeriodDays": 3650 }
  ```
- **Gemini CLI** は 30 日以上のセッションを削除します。`~/.gemini/settings.json` で無効化してください。プロジェクトに `.gemini/settings.json` がある場合は、ワークスペース設定がユーザー設定を上書きするため、そこでも同じ変更をしてください:
  ```json
  { "general": { "sessionRetention": { "enabled": false } } }
  ```

その他のすべてのサポートクライアントはデフォルトで履歴を永久に保持します。クライアント別の完全調査、修正の詳細、ローカル SQLite インデックスが保存できること / できないことについては、**[docs/reference/HISTORY_RETENTION.md](docs/reference/HISTORY_RETENTION.md)** を参照。

## ロードマップ

`docs/development/ROADMAP.md` を参照。

## コントリビューティング / セキュリティ

- コントリビューティングガイド: `docs/CONTRIBUTING.md`
- セキュリティポリシー: `docs/SECURITY.md`

## ドキュメント

完全なドキュメントは **[`docs/`](docs/README.md)**（インデックスから開始）にあり、次のグループに分かれています:

- **[guides/](docs/guides/)** — 課題指向のセットアップ: オンボーディング、リモートアクセス、ステータスライン、バックグラウンドサービス。
- **[reference/](docs/reference/)** — 参照資料: API 参照、対応クライアント、履歴保持。
- **[development/](docs/development/)** — チェンジログ、リリース、ロードマップ、公開の `technical-notes/`。

## プロジェクト構成

```
tokdash/
├── main.py                 # ソースのエントリポイント (python3 main.py)
├── tokdash                 # ソースの CLI ラッパー (./tokdash serve)
├── src/
│   └── tokdash/
│       ├── cli.py
│       ├── api.py                # FastAPI ルート / アプリ
│       ├── compute.py            # 集約 / マージロジック
│       ├── dateutil.py           # 共用の日付範囲パース
│       ├── sessions.py           # セッションエクスプローラーロジック
│       ├── pricing.py            # PricingDatabase ラッパー
│       ├── assets.py             # 静的アセット管理
│       ├── model_normalization.py
│       ├── pricing_db.json
│       ├── sources/
│       │   ├── openclaw.py       # OpenClaw セッションログパーサー
│       │   └── coding_tools.py   # ローカルコーディングツールパーサー
│       └── static/
│           ├── index.html        # シングルページダッシュボード
│           ├── theme-config.js   # テーマパレットとヒートマップカラー
│           └── themes.css        # テーマごとの CSS オーバーライド
└── docs/                   # ドキュメント — インデックスは docs/README.md
    ├── guides/             # オンボーディング、リモートアクセス、ステータスライン、バックグラウンドサービス
    ├── reference/          # API 参照、対応クライアント、履歴保持
    └── development/        # チェンジログ、リリース、ロードマップ、technical-notes/
```

## ライセンス

MIT License - `LICENSE` を参照。
